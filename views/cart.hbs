<h1>Корзина</h1>

{{#if items.length}}
<table class="js-cart-table">
  <tr>
    <th>Название</th>
    <th>Количество</th>
    <th>Цена</th>
    <th align="right">Действие</th>
  </tr>
  {{#each items}}
  <tr>
    <td>{{this.name}}</td>
    <td>{{this.count}}</td>
    <td>{{this.price}}</td>
    <td align="right">
      <button class="btn" data-id="{{this._id}}">Удалить</button>
    </td>
  </tr>
  {{/each}}
  <tr>
    <td></td>
    <td></td>
    <td></td>
    <td align="right" colspan="4">Итог - {{total}}</td>
  </tr>
</table>

<form action="/orders" method="POST" style="margin-top: 16px">
  <button class="btn">Оформить заказ</button>
</form>
{{else}}
<p>Корзина пуста</p>
{{/if}}